# 2026-05-17 — goals/sisyphus routing

## Goal

Add repository-level guidance so agents offer `/goals` or `/sisyphus` before starting tasks with multiple actions or higher complexity.

## Change

- Added a new `Goal/Sisyphus routing` section to `AGENTS.md`.
- Defined when to suggest `/goals` versus `/sisyphus`.
- Required explicit user confirmation before creating a goal/sisyphus draft.
- Kept simple one-action tasks as direct execution by default.

## Validation

- Read `AGENTS.md` before editing.
- Applied a focused documentation edit only.
